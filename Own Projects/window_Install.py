
width = int(input("Width (meters): "))
height = int(input("Length (meters): "))

area = width * height

# glass sold per square meter
# wood sold per foot

if 0.5 > width > 3.5:
    # width must be between 0.5 and 3.5 inclusive
    # ask for user input again, maybe use def?

if 0.5 > height > 2.0:
# height must be between 0.5 and 2.0 inclusive
# ask for user input again, maybe use def?